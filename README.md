# Splitter

Splitter is a command-line tool that takes a number of files, splits them into smaller chunks, and creates a script for
reassembling those chunks into files on the other side.  Optionally, the chunks can be sent through SSH to a remote
host in parallel.

